{
  "defaults": {
    "name": "{{classname}} > {{name}}",
    "testSuite": "{{classname}}",
    "testSuiteSection": "General",
    "automationId": "{{name}}",
    "customFields": {
      "priority": "Low"
    }
  }
}
